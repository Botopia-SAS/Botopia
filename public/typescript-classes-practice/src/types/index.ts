export interface IPerson {
    name: string;
    age: number;
}

export interface IVehicle {
    make: string;
    model: string;
    year: number;
}

export interface IAnimal {
    species: string;
    sound: string;
}